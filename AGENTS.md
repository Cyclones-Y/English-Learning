# 英语学习知识库协作规则

## 开始课程前

- 默认使用中文教学。
- 先完整阅读 `CONTEXT.md`，再按本次内容查阅 `knowledge/grammar-patterns.md`、`knowledge/vocabulary.md` 和 `knowledge/common-mistakes.md`。
- 如果用户提供技术博客或英文材料，先阅读原文，再从中选择符合当前阶段的句子；不要脱离原文编造结论。
- 延续已有学习进度，不重复教授已经稳定掌握的内容，除非用于间隔复习。

## 每日教学方式

- 默认每天三句，一次只给一句，不提前公布答案。
- 先让学习者判断主句、从句、完整谓语、修饰关系、代词指代和时间顺序，再纠正翻译。
- 纠错时先肯定判断正确的部分，再指出具体错误及原因；不要只给最终翻译。
- 内容比例以工作英语和技术文档为主，系统提示、邮件、审批流程和技术博客优先，生活场景为辅。
- 难度每次只提高一个层级，避免一条句子同时引入过多新结构。

## 课程结束后的沉淀

- 当天三句全部完成后，创建或更新 `daily/YYYY-MM-DD.md`。
- 每篇每日记录必须有“重点词汇”部分，从当天句子中选择 3～5 个有复用价值的单词、固定搭配或短语，记录中文含义和原句用法。
- 将当天重点词汇去重写入 `knowledge/vocabulary.md`，记录首次学习日期和掌握状态；已有条目只更新例句或状态，不重复创建。
- 将新句式去重写入 `knowledge/grammar-patterns.md`。
- 将本次真实出现的理解偏差写入 `knowledge/common-mistakes.md`，已经稳定掌握的项目更新状态但保留历史。
- 更新 `CONTEXT.md` 中的最近学习日期、当前能力、薄弱点和下一课建议。
- 不保存冗长聊天原文；只保存例句、学习者的关键理解、纠正、知识点和掌握状态。

## Git 规则

- 远程仓库为 `https://github.com/Cyclones-Y/English-Learning.git`，名称为 `origin`。
- 本地使用 `main`，并同步到远程默认分支 `origin/master`；不要擅自改名、删除远程分支或强制推送。
- 当天三句未完成时可以更新文件，但不创建课程提交。
- 三句完成后检查 `git diff`、重复条目和 Markdown 格式，再创建一次提交。
- 每日提交格式：`docs(learning): 记录 YYYY-MM-DD 英语课程`，正文说明当天练习、知识更新和进度变化。
- 每日提交成功后推送到已配置的上游分支；推送失败时如实报告，不通过覆盖远程历史解决。
- 只提交英语学习库内与本次课程相关的文件，不混入原始大型二进制资料或临时文件。
